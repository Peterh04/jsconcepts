/**
 * @var
* var can be reassign and redeclare
 var name = " Dan"
 var ]name = "James"

@let
You can reassign

let name = "James"
name = "Dan"

@const
 neither reassign or redeclare

 @Strings declare characters  e.g "Dan" "1" must be in quotation marks
 @Numbers declare numbers e.g 1, 1.5
 @Boolean declare values either trie or false
 @Null  absence of a value, e.g., null.
@Undefined  Rvariable that has been declared but not assigned a value, e.g., undefined.
 * 


@listens. array represents list [1, 2, 3]
@  Represents key-value pairs, e.g., { name: "John", age: 30 }.
 */

/** Mathematics 
 * @Modulus picks the reminder
 * 
 * 
 * 
 */

//Revenue
//Expense
//Cost of goods





let length = prompt("Enter rectangle Length")
 let  width = prompt("Enter rectangle width")

 parseFloat(length)
parseFloat(width)

let rectangle_Area = length * width
let rectangle_perimeter = (length * 2) + (width * 2)



console.log("Rectangle's perimeter is ", (rectangle_perimeter))
console.log("Rectangle's area is" ,( rectangle_Area))
console.log(typeof rectangle_perimeter);























